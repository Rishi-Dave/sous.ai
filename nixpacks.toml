providers = []

[phases.setup]
nixPkgs = ["python312", "uv"]

[phases.install]
cmds = ["cd backend && uv sync --frozen"]

[start]
cmd = "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port $PORT"
